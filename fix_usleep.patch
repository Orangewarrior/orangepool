diff --git a/tests/test_drain.c b/tests/test_drain.c
index 1111111..2222222 100644
--- a/tests/test_drain.c
+++ b/tests/test_drain.c
@@ -1,8 +1,17 @@
+#define _POSIX_C_SOURCE 200809L
+
 #include "../include/orangepool.h"
 #include "test_common.h"
 #include <stdatomic.h>
-#include <unistd.h>
+#include <time.h>
+
+static void sleep_us(long us) {
+    struct timespec ts;
+    ts.tv_sec = us / 1000000L;
+    ts.tv_nsec = (us % 1000000L) * 1000L;
+    nanosleep(&ts, NULL);
+}
 
 static atomic_int counter = 0;
 
 static void *task(void *arg) {
     (void)arg;
     atomic_fetch_add(&counter, 1);
-    usleep(1000);
+    sleep_us(1000);
     return NULL;
 }
diff --git a/tests/test_poison_semantics.c b/tests/test_poison_semantics.c
index 1111111..2222222 100644
--- a/tests/test_poison_semantics.c
+++ b/tests/test_poison_semantics.c
@@ -1,8 +1,17 @@
+#define _POSIX_C_SOURCE 200809L
+
 #include "../include/orangepool.h"
 #include "test_common.h"
 #include <stdatomic.h>
-#include <unistd.h>
+#include <time.h>
+
+static void sleep_us(long us) {
+    struct timespec ts;
+    ts.tv_sec = us / 1000000L;
+    ts.tv_nsec = (us % 1000000L) * 1000L;
+    nanosleep(&ts, NULL);
+}
 
 static atomic_int counter = 0;
 
 static void *task(void *arg) {
     (void)arg;
     atomic_fetch_add(&counter, 1);
-    usleep(5000);
+    sleep_us(5000);
     return NULL;
 }
diff --git a/tests/test_queue_overflow.c b/tests/test_queue_overflow.c
index 1111111..2222222 100644
--- a/tests/test_queue_overflow.c
+++ b/tests/test_queue_overflow.c
@@ -1,7 +1,16 @@
+#define _POSIX_C_SOURCE 200809L
+
 #include "../include/orangepool.h"
 #include "test_common.h"
-#include <unistd.h>
+#include <time.h>
+
+static void sleep_us(long us) {
+    struct timespec ts;
+    ts.tv_sec = us / 1000000L;
+    ts.tv_nsec = (us % 1000000L) * 1000L;
+    nanosleep(&ts, NULL);
+}
 
 static void *slow_task(void *arg) {
     (void)arg;
-    usleep(200000);
+    sleep_us(200000);
     return NULL;
 }
diff --git a/tests/test_immediate_shutdown.c b/tests/test_immediate_shutdown.c
index 1111111..2222222 100644
--- a/tests/test_immediate_shutdown.c
+++ b/tests/test_immediate_shutdown.c
@@ -1,9 +1,18 @@
+#define _POSIX_C_SOURCE 200809L
+
 #include "../include/orangepool.h"
 #include "test_common.h"
 #include <stdatomic.h>
 #include <stdlib.h>
-#include <unistd.h>
+#include <time.h>
+
+static void sleep_us(long us) {
+    struct timespec ts;
+    ts.tv_sec = us / 1000000L;
+    ts.tv_nsec = (us % 1000000L) * 1000L;
+    nanosleep(&ts, NULL);
+}
 
 static atomic_int destroyed = 0;
 
 static void *slow_task(void *arg) {
     int *p = (int *)arg;
-    usleep(30000);
+    sleep_us(30000);
     free(p);
     return NULL;
 }
@@ -24,7 +33,7 @@ int main(void) {
         *p = i;
         ASSERT_EQ_INT(orangepool_submit(pool, slow_task, p, dtor), ORANGEPOOL_OK);
     }
 
-    usleep(5000);
+    sleep_us(5000);
     ASSERT_EQ_INT(orangepool_shutdown(pool, ORANGEPOOL_SHUTDOWN_IMMEDIATE), ORANGEPOOL_OK);
     ASSERT_TRUE(atomic_load(&destroyed) >= 1);
 
diff --git a/tests/test_timeout.c b/tests/test_timeout.c
index 1111111..2222222 100644
--- a/tests/test_timeout.c
+++ b/tests/test_timeout.c
@@ -1,7 +1,16 @@
+#define _POSIX_C_SOURCE 200809L
+
 #include "../include/orangepool.h"
 #include "test_common.h"
-#include <unistd.h>
+#include <time.h>
+
+static void sleep_us(long us) {
+    struct timespec ts;
+    ts.tv_sec = us / 1000000L;
+    ts.tv_nsec = (us % 1000000L) * 1000L;
+    nanosleep(&ts, NULL);
+}
 
 static void *slow_task(void *arg) {
     (void)arg;
-    usleep(200000);
+    sleep_us(200000);
     return NULL;
 }
diff --git a/tests/test_blocking_submit.c b/tests/test_blocking_submit.c
index 1111111..2222222 100644
--- a/tests/test_blocking_submit.c
+++ b/tests/test_blocking_submit.c
@@ -1,8 +1,17 @@
+#define _POSIX_C_SOURCE 200809L
+
 #include "../include/orangepool.h"
 #include "test_common.h"
 #include <pthread.h>
-#include <unistd.h>
+#include <time.h>
+
+static void sleep_us(long us) {
+    struct timespec ts;
+    ts.tv_sec = us / 1000000L;
+    ts.tv_nsec = (us % 1000000L) * 1000L;
+    nanosleep(&ts, NULL);
+}
 
 static void *slow_task(void *arg) {
     (void)arg;
-    usleep(60000);
+    sleep_us(60000);
     return NULL;
 }
